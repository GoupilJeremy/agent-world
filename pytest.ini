[pytest]
# Configuration pour pytest

# Répertoires de tests
testpaths = tests

# Fichiers de tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Options pytest (fusionnées)
addopts = -v --tb=short --cov=backend --cov-report=term-missing --cov-report=html --cov-fail-under=90 -p no:warnings

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks integration tests
    unit: marks unit tests
    api: marks API endpoint tests

# Fichiers à ignorer
norecursedirs = venv .venv .git __pycache__ *.egg-info

# Configuration de la sortie
console_output_style = progress
showlocals = True
color = yes

# Ignorer les avertissements
filterwarnings = ignore::DeprecationWarning
